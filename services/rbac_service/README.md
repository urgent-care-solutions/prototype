# RBAC Service
This service is responsible for RBAC management within the system. Its only responsibility is the RBAC itself. This service cannot and should not authenticate users: it only handles the authorizattion
